server:
  port: 8081
spring:
  application:
    name: springboot-mp
  datasource:
    username: wan
    password: 101101
    url: jdbc:mysql://localhost:3306/db_mybatis-plus?useSSL=false&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
# ????
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  # ??????
  global-config:
    db-config:
      logic-delete-field: flag
      logic-delete-value: 1
      logic-not-delete-value: 0

